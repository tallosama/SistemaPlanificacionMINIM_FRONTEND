export class Control {
  // public static evaluarErrorRepetido(mensaje: string): string {
  //   switch (mensaje) {
  //     case "could not execute statement; SQL [n/a]; constraint [null]; nested exception is org.hibernate.exception.ConstraintViolationException: could not execute statement":
  //       return " se detectó 'No pueden haber registros repetidos'";
  //     case "No value present":
  //       return " se detectó 'No se encontró el registro'";
  //     default:
  //       return " '" + mensaje + "'";
  //   }
  // }
  // public static evaluarErrorDependiente(mensaje: string): string {
  //   switch (mensaje) {
  //     case "could not execute statement; SQL [n/a]; constraint [null]; nested exception is org.hibernate.exception.ConstraintViolationException: could not execute statement":
  //       return " se detectó 'El registro posee dependencias'";
  //     case "No value present":
  //       return " se detectó 'No se encontró el registro'";
  //     default:
  //       return " '" + mensaje + "'";
  //   }
  // }
}
